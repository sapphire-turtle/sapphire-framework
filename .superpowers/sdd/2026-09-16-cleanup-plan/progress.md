# Step 11 (cleanup) Task 1: complete (commit 98b36a8, self-reviewed). sapphire-framework-keys extracted: KeyStore/auth/protect+AuthConfig moved via git mv (R094), 5 extraction tests, real API preserved (5-arg generate + device_id - brief sketch was lossy; sapphire-agent depends on device_id), AuthConfig replaces bare Arc<KeyStore> for fail-closed-503/insecure_for_tests pins, grain-id dep added, facade keys feature, axum optional (default build links 0 axum). 66 targets 0 failed; known flakes flaked once mid-run passed standalone.
# Step 11 (cleanup) Task 2: complete (commit ea5fb2c, self-reviewed). 4 crates deleted (rpc/remote-client/remote-server/blob, -4660 lines), surface.rs 2 verbatim tests, backend RemoteBackend/remote.rs/Remote+Conflict errors/WorkspaceSource::Remote removed (WorkspaceLocator Path-only), gui remote dialog removed (scope extension - gui used remote url form; old configs with url now error on locator()), facade native feature minimal-coherent (full rework=Task 4), release-plz updated. 678 passed 0 real failed.
# Step 11 (cleanup) Task 3: complete (commit aa028dd, self-reviewed). migrate_app_dir replaced by unsplit_app_dir in workspace crate: app dirs flatten to <root>/<app>/ (AppKind never in a path, env override replaces platform root only), server-wins conflict policy losers left in place + warned, rename→warn (move_item/copy_then_delete kept for keys migration), kind dir removed only when emptied, migrate_keys_to_data unchanged and runs FIRST in init (per-kind cache layout still present), then unsplit on cache/data/config. 9 brief tests + env-var test adapted to TestEnv helper (data/config pinned to tempdirs). 677 passed 0 real failed (converge flaked at HEAD-with-changes and clean-HEAD control alike; standalone 5/5).
# Step 11 (cleanup) Task 3: complete (commit aa028dd, self-reviewed). unsplit_app_dir replaces migrate_app_dir; paths <root>/<app>/ (no kind layer); keys-migration runs before unsplit (fix: data kind dir recreated only when migrating); 9 unsplit tests + TestEnv adaptation; 45/45 workspace x5, full suite 677 passed 0 real.
# Step 11 (cleanup) Task 4: complete (commit 9ef7e4f, self-reviewed). native drops keys (plan's exact set), features.rs 3 tests (mutation-verified RED), CI per-feature cargo check loop step added to test job (--locked added), 12 features standalone green. Task 2 had already pre-cleaned most facade deps.
