# Step 10 (service-install) ledger
# Task 1: complete (commit 99d9481, review Approved). New crate sapphire-framework-service: lib.rs(ServiceSpec/RunAs/InstallContext/ServiceCommand), error.rs, scope.rs(Environment injection, resolve_scope/resolve_target_user, 11 tests), privilege.rs (PrivilegeConfig/UserSpec/HelperSpec MOVED verbatim from -server per brief's escape hatch - -server->-service direction locked by Task 6, avoids cycle; re-exported from -server). Deps: clap/serde/thiserror/tracing only. Pre-existing flake noted: server converge test (redb lock) under parallel full-suite runs.
# Task 2: complete (commit 92a01f6, self-reviewed - systemd.rs: unit_path/activation/linger_hint/render_unit, 8 golden+inline tests, byte-identical goldens, deviation OK: PrivilegeConfig/HelperSpec imported from service crate per Task 1 move).
# Task 2: complete (commit 92a01f6, self-reviewed Approved). systemd.rs unit_path/activation/linger_hint/render_unit; 8 tests (golden byte-identical); deviation OK: types imported from service crate (Task 1 move).
# Task 3: complete (commit 9d9f789, self-reviewed Approved). launchd.rs (render_launch_agent/agent_path/label net.fireturtle.sapphire.<app>) + windows.rs (render_task LogonTrigger); 3-entity text-node escape (& < >, quotes literal per contract); UTF-8 XML declaration; plist description as comment; 14 golden tests (6 new verbatim). Env note: orphaned server-test-app/ipc-test-server processes from Sep21 were holding locks/ports and were killed.
