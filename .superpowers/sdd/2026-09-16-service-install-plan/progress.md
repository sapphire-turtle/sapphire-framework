# Step 10 (service-install) ledger
# Task 1: complete (commit 99d9481, review Approved). New crate sapphire-framework-service: lib.rs(ServiceSpec/RunAs/InstallContext/ServiceCommand), error.rs, scope.rs(Environment injection, resolve_scope/resolve_target_user, 11 tests), privilege.rs (PrivilegeConfig/UserSpec/HelperSpec MOVED verbatim from -server per brief's escape hatch - -server->-service direction locked by Task 6, avoids cycle; re-exported from -server). Deps: clap/serde/thiserror/tracing only. Pre-existing flake noted: server converge test (redb lock) under parallel full-suite runs.
# Task 2: complete (commit 92a01f6, self-reviewed - systemd.rs: unit_path/activation/linger_hint/render_unit, 8 golden+inline tests, byte-identical goldens, deviation OK: PrivilegeConfig/HelperSpec imported from service crate per Task 1 move).
# Task 2: complete (commit 92a01f6, self-reviewed Approved). systemd.rs unit_path/activation/linger_hint/render_unit; 8 tests (golden byte-identical); deviation OK: types imported from service crate (Task 1 move).
# Task 3: complete (commit 9d9f789, self-reviewed Approved). launchd.rs (render_launch_agent/agent_path/label net.fireturtle.sapphire.<app>) + windows.rs (render_task LogonTrigger); 3-entity text-node escape (& < >, quotes literal per contract); UTF-8 XML declaration; plist description as comment; 14 golden tests (6 new verbatim). Env note: orphaned server-test-app/ipc-test-server processes from Sep21 were holding locks/ports and were killed.
# Task 4: complete (commit 937d646, self-reviewed Approved). manager.rs: ServiceManager trait (write_unit/run/remove_unit), SystemManager (only impl that shells out), RecordingManager (records+canned output), ServiceCommand{Install(Uninstall,Status)}/InstallArgs{user,system,run_as,keep_helper}, install/uninstall/status dispatch by env.os (Linux systemctl / macOS launchctl bootstrap gui/<uid> / Windows schtasks), install cleans up half-written unit on failed activation, home_dir only where needed (system/Windows don't need HOME). Deviation accepted: guard test needle via concat!("System","Manager") (verbatim version unsatisfiable - test text itself contains token 3x; mechanism+threshold preserved). 39 lib + 14 golden tests green; full suite green (known converge flake reproduced as pre-existing).
# Task 5: complete (commit 5d9e921, self-reviewed Approved). post_install runs after activation with resolved InstallContext; keep_helper skips; failing hook fails install with error+"installed and running" message; privsep beats InvokingUser (already true via resolve_target_user, test locks it). chown via new ServiceManager::chown_to_user default-no-op trait method (SystemManager: getpwnam+chown cfg(linux)), called on unit file for Linux system installs with target user - minimal documented reading. New dep libc (target-gated). Verbatim tests needed 3 compiler-forced mechanical fixes (Vec<String> vs &str turbofish). 44 lib + 14 golden green; full suite green.
# Task 6: complete (commit 56029b5, self-reviewed). ServiceCommand wired into ServerCommand + BridgeCommand; AppServer::privileges/service_spec; Environment::detect added; bridge lib re-exports ServiceCommand/ServiceSpec (apps/bridge needs no direct service dep); ARCHITECTURE.md crate-table row. Full suite 752 passed 0 failed.
# STEP 10 (service-install) COMPLETE: Tasks 1-6 done. Commits: 99d9481 (crate+scope), 92a01f6 (systemd), 9d9f789 (launchd/windows), 937d646 (manager), 5d9e921 (post_install/privsep), 56029b5 (app wiring).
