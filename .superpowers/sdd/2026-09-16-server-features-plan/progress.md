# Step 9 progress ledger (server-features). Branch feat/p2p-sync-iroh.
# Plan: docs/superpowers/plans/2026-09-16-server-features-plan.md. Depends on steps 6-8 (done).
# Step 9 review carried forward: step-level final review per step as before.
# Task 1: complete (commit 8ac1ac3, review Approved-with-caveats-Minor). LiveSession in session crate: Message::Live(Vec<PathUpdate>); open_live_session = initial exchange then reader stays running; push/updates/peer_vv/is_open/close. session.rs refactored (initial_exchange returns Exchange<S>, refuse helper on early returns) - run_session behaviorally unchanged, step-7 tests pass unchanged. Batch published only AFTER content settled (Pending/Blob or Missing releases batch) - prevents middle host announcing entries it can't serve. peer_vv merges each batch's seen. 7 live tests (6 brief + 1 added fetch-by-hash). Dead peer: push fails immediately, write half separate task. Minor caveats accepted: brief lock-during-merge, low-risk wanted.insert race, Error::Protocol on early peer leave (live session meaningless if exchange incomplete - acceptable).
# Task 1 (session crate): see above.
# Task 2: complete (commit 852dc21, review Approved-with-caveats-Minor). Live propagation in app server: LivePeers (sessions keyed by device id), SyncRuntime::after_commit->fan_out(from None), dial_loop(self:Arc) keeps sessions open w/ per-peer exponential backoff capped DIAL_BACKOFF_MAX=5min. Storm rules both tested (from-exclusion live.rs:90-92, peer_vv filter live.rs:93-96). ONE-WAY DIALING deviation accepted: host dials only peers with GREATER device id (in dial_loop AND sync_now) - makes waits-for graph a DAG, fixes flaky settle (replica mutex held across exchange made simultaneous dialing a circular wait); tie-break (LivePeers::insert keeps stream initiated by LOWER device id) is safety net. sync_now switched from one-shot run_session to sync_one->adopt_live_session (was killing inbound live sessions). Fixtures: LoopbackNetwork::partitioned, drop_connections, frames_sent counter (frame counter behind test-util), settle, synced_pair/synced_triple. Caveat accepted: fan_out holds tokio sessions-lock across push await (no deadlock, push doesn't need same lock; collect-drop-await would be cleaner - minor). Live tests 6/6 stable 8+ parallel runs.
# Task 2 review (independent, handle 01a0c2d7): Spec ✅ (all items). Code quality: Approved-with-caveats(Minor) - fan_out holds tokio sessions-lock across push await (no deadlock, push needs no same lock; collect-drop cleaner), clone-per-iteration nit. One-way dialing judged coherent with "every peer": both session directions exist, only one side dials; redial test holds.
# Task 3: complete (commit fbd2baa, review Approved-with-caveats-Minor). Relay config: relay.rs RelayConfig{urls,use_default}/relays(host,workgroup) merge-not-override (dedup BTreeMap; use_default only false if BOTH sides false; url::Url::parse validation incl. unknown-scheme rejection); NetConfig.use_default_relays (Default derive default true); workgroup published_net/publish_net (root/net.toml, write_atomic from routes.rs). CRITICAL iroh subtlety handled: RelayMode::Custom REPLACES the whole relay set, so use_default=true folds iroh::defaults::prod::default_relay_map() into the Custom map (no Default-+Custom combination in iroh 1.2) - regression test a_custom_map_with_use_default_keeps_the_default_relays_too. 8 relay tests + 1 new net.rs test; full suite 648/0. Minor: duplicate url::2 direct dep (already transitive via iroh - fine), tests need #[allow(dead_code)] on test-only NetConfig field. PRE-EXISTING FLAKE (do not attribute to steps): server::converge::a_host_that_was_offline_catches_up_when_it_returns ~50% fail at HEAD~1 too - timing-sensitive await_file; flagged for follow-up.
# Task 3: complete (commit fbd2baa, review Approved). relay.rs RelayConfig{urls,use_default}+relays() merge-not-override (dedup, use_default false only if BOTH sides, Url::parse validation incl unknown-scheme); NetConfig.use_default_relays (default true); Workgroup published_net/publish_net (net.toml in workgroup root). iroh subtlety: RelayMode::Custom replaces relay set -> use_default folds prod default_relay_map into Custom map (regression test). 8 relay tests+1 net.rs test; suite 648/0.
# Task 4: complete (commit 779fd37, review Approved). Embedded relay behind `embedded-relay` cargo feature (OFF default; app passthrough feature). EmbeddedRelay::start/stop/url; EmbeddedRelayConfig{bind,hostname,allow_loopback,tls: TlsConfig}; NetConfig.embedded_relay: Option. Start checks: loopback refused w/o allow_loopback (error says reachable), hostname required. iroh 1.2 DOES offer clear_ip_transports() - meet-through-relay test runs relay-only (no #[ignore]). Beyond-brief additions judged sound: IrohTransport::new_relay_only (public new unchanged), NodeAddr.relay_urls (additive serde default Vec::new, used via add_known_address TransportAddr::Relay - step 8 pairing/sync OK). Embedded relay URL feeds same relay_mode_for path (no 2nd construction path). 5 embedded_relay tests; Cargo.lock +19 pkgs (iroh-relay server feature).
# Task 5: complete (commit 86d3a26, review Approved-with-caveats-Minor). status.json: StatusFile/PeerStatus/RouteStatus/StatusWriter(5s+on-change via 1s poll-diff)/StatusSource; atomic via routes::write_atomic (pid-only temp name - safe under single-instance lock); bridge status = live control-plane call, file fallback when no bridge; wired in serve_loops. 9 status tests + 4 integration + 2 CLI; env-race fixed via shared test_env lock (also fixes pre-existing flake source). Caveats accepted (pid-only temp name; writer only in serve_loops by design).
# Task 6: complete (commit fb340f8, review Approved-with-caveats-Minor). logging.rs: LOG_FILE/LOG_MAX_BYTES(10MiB)/LOG_KEEP(3); install(&dir)->LogGuard via once-only global registry with swappable wire (set_global_default is once-only - verified); custom size-rotating writer over tracing-appender::non_blocking; append-continuous across restarts; console layer preserved (install_console() shared default filter with main.rs). BridgeCommand::Log{follow,lines}; tail reads current file only. 6 tests (5 brief verbatim + follow); 677 suite passed. Caveat accepted: try_init result ignored - low risk (single-binary early init).
# Task 7: complete (commit 504d7f2, self-reviewed inline - 12-line diff verbatim per brief). the_log_subcommand_parses added; ARCHITECTURE.md operational-surface note (blockquote placed after full crate table - mid-table insertion would break markdown, sound deviation). FULL STEP 9 COMPLETE (Tasks 1-7).
