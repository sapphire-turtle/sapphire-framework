# Step 11 (cleanup) Task 1: complete (commit 98b36a8, self-reviewed). sapphire-framework-keys extracted: KeyStore/auth/protect+AuthConfig moved via git mv (R094), 5 extraction tests, real API preserved (5-arg generate + device_id - brief sketch was lossy; sapphire-agent depends on device_id), AuthConfig replaces bare Arc<KeyStore> for fail-closed-503/insecure_for_tests pins, grain-id dep added, facade keys feature, axum optional (default build links 0 axum). 66 targets 0 failed; known flakes flaked once mid-run passed standalone.
